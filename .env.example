# ==========================================
# VANTAGE BRIDGE CONFIGURATION
# ==========================================
# Rename this file to '.env' and fill in your details.

# --- Vantage Controller Connection ---
VANTAGE_HOST=192.168.1.x
VANTAGE_USER=
VANTAGE_PASS=

# --- MQTT Broker Connection ---
# If using Home Assistant OS, host is often 'core-mosquitto'
MQTT_HOST=127.0.0.1
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TLS_ENABLED=false

# --- "Sniper" Tuning (Performance Settings) ---

# POLL_INTERVAL (Seconds)
# How often to force a full status check if no activity is detected.
# Default: 90 (Safe background heartbeat)
POLL_INTERVAL=90

# POLL_QUIET_TIME (Seconds)
# Prevents polling if the system was active recently.
# Recommended: Set this to your longest scene fade time + 1 second.
# Default: 5
POLL_QUIET_TIME=5

# COMMAND_THROTTLE_DELAY (Seconds)
# Critical for older controllers. Adds a tiny pause between outgoing commands
# to prevent the Serial Buffer from overflowing and dropping packets.
# Default: 0.02 (20ms) is invisible to the eye but safe for hardware.
COMMAND_THROTTLE_DELAY=0.02

# --- Debugging ---

# PUBLISH_RAW_BUTTON_EVENTS (true/false)
# If true, publishes raw JSON to 'vantage/keypad/_raw' for every button press.
# Useful for finding Keypad IDs, but noisy for production.
# Default: false
PUBLISH_RAW_BUTTON_EVENTS=false

# LOG_LEVEL (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
